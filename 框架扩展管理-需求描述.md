# 框架扩展管理 — 需求描述

## 页面入口
侧边栏「模型训练 → 框架扩展」，统一管理微调框架的自定义扩展。

---

## 核心概念

扩展 = 一个沙箱化的代码包 + 一份参数 Schema + 一个标准化入口函数。本质是对训练框架的 Hook 注入：

| 扩展类型 | 注入点 | 做什么 |
|----------|--------|--------|
| 微调算法 | 训练循环启动前 | 替换 optimizer / loss / lr_scheduler，注入 LoRA 等算法 |
| 优化器 | 训练循环启动前 | 替换默认 optimizer（如 AdamW → 自定义 Adam） |
| 数据处理 | 数据集加载后、喂入模型前 | 逐样本预处理、去重、过滤、格式转换 |
| 评估方法 | 每个 epoch 结束、最终评估时 | 自定义评估指标计算和汇总 |

生命周期：模板下载 → 本地开发 → 打包上传 → 平台串行验证（兼容→安全→单元→集成→性能）→ 全部通过后配置参数并启用 → 创建训练任务时可选用。

---

## 1. 标准模板库

内置 4 个标准接口模板，每张卡片展示类型标签 + 名称 + 功能描述，3 个按钮：

| 按钮 | 行为 |
|------|------|
| 代码示例 | 弹窗展示：适用场景标签 + 3 步开发引导 + Python 代码 + 参数名/默认值/说明表 |
| 下载模板 | 下载 .txt，含名称、步骤指引、参数列表及其默认值 |
| 基于此模板上传 | 打开文件选择器，上传后扩展继承该模板的类型和参数 Schema |

4 个模板：
- LoRA 算法扩展（微调算法）
- 自定义优化器（优化器）
- 对话数据处理（数据处理）
- 领域评估方法（评估方法）

---

## 2. 上传与验证

**上传**：支持 .zip / .tar.gz / .tgz。基于模板上传时扩展继承模板类型和参数 Schema；直接上传时默认关联首个模板。

**验证表格**：按扩展名称分组合并，每组显示最新版本验证状态。5 列图标：✅通过  ❌失败  ⏳验证中  ○等待。

串行执行——前一项通过才执行后一项，任一失败则后续保持等待。五类验证：兼容性检查、安全性检查、单元测试、集成测试、性能验证。

操作列规则：全通过 →「启用」按钮 +「调试」按钮 +「新版本」按钮；任一失败 →「失败」红字；验证中 →「等待」灰字；已启用 →「已启用」绿标签。

**边界情况**：取消文件选择不产生记录；同名文件上传视为新版本加入对应分组。

---

## 3. 版本控制

同名扩展多版本共存，按名称分组合并展示。每组可展开查看历史版本列表。

版本规则：
- 同一扩展名只能启用一个版本（启用新版本时自动停用旧版本）
- 每组最多保留 10 个历史版本
- 已验证通过的版本可设为当前启用版本
- 「新版本」按钮绑定分组，上传后自动加入该组

操作：
- 默认展示最新版本及验证状态
- 点击展开箭头显示所有历史版本
- 历史版本行可启用或调试（仅通过验证的版本）

---

## 4. 参数配置与启用

点击验证通过的扩展行展开参数配置区。参数由模板预定义（如 LoRA Rank、Alpha 等）。控件类型：数字输入框（带 min/max/step）、下拉框、文本输入框。

修改即时生效，确认启用时提交最终值。

点击「确认启用」后：
- 该扩展出现在监督微调配置页的「已验证扩展组件」区域
- 提供独立开关控制是否在本次训练任务中使用
- 扩展配置随训练任务 JSON 提交

---

## 5. 与训练任务的集成

训练配置页（创建训练任务 → 步骤 4 → 展开「高级配置」→ Gang Scheduling 下方）：

- 列出所有已启用的扩展，每行一个开关（默认全开）
- 点击「管理扩展」跳转回框架扩展页

提交训练任务时 JSON 附带扩展配置：

```json
{
  "extensions": [
    { "name": "LoRA+ 训练器", "type": "微调算法", "version": "v1.2.0", "parameters": { "rank": 16, "alpha": 32 } }
  ]
}
```

调度器启动训练前按类型排序加载扩展包并执行入口函数。

---

## 6. 调试工具

验证通过的扩展可进入调试沙箱环境。入口：扩展版本列表操作列「调试」按钮。

调试环境：
- 平台分配独立沙箱容器，预装训练框架基础依赖
- 注入标准测试数据集（平台预置 3 类：对话样本 100 条、文本补全 50 条、图文对 30 条）
- 浏览器终端中查看执行日志和输出

沙箱限制：
- 最大运行 30 分钟，超时自动终止
- 网络仅允许访问平台内部服务，禁止外网
- 调试结束后沙箱销毁

---

## 7. 本期不做

- 模板管理后台（增删改）、在线代码编辑器
- 独立的测试报告详情页、独立的教程文档页面
- 扩展停用/卸载、版本对比 diff
- 真实 CI/CD 验证流水线（当前 mock 图标）
