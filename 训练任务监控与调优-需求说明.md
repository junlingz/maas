# 训练任务监控与调优PRD

## 一、功能需求总览

| 模块 | 功能项 | 功能点 | 需求类型 | 优先级 |
|---|---|---|---|---|
| 用户端-模型训练 | 训练前配置 | 梯度聚合频率设置 | 新增 | P0 |
| 用户端-训练监控 | 全局训练状态 | 训练指标与趋势图表 | 新增 | P0 |
| 用户端-训练监控 | 节点性能分析 | 节点性能表格与排序 | 新增 | P0 |

本次采用最小开发范围：

- 「聚合策略微调」定义为训练启动前修改梯度聚合频率。
- 「应用到后续训练」定义为将提交的频率写入当前任务配置，并在该任务启动后全程使用。
- 训练启动后聚合策略只读，不支持运行中热更新、暂停后更换或自动回滚。
- 「观察策略影响」定义为在同一看板中查看当前聚合频率、Loss、验证准确率、吞吐率及节点网络指标；系统不自动生成因果结论。
- 不增加策略状态机、安全同步点、调整前后自动对比、多任务对比和新告警引擎。

## 二、详细功能需求

### 2.1 梯度聚合频率设置

- **页面入口**：「模型训练 → 创建训练任务 → 训练参数 → 分布式聚合策略」。
- **核心功能**：用户在训练启动前通过一个数字输入框设置梯度聚合频率，提交后该值应用于本次训练。

- **业务流程**：
  - **用户操作流程**：进入创建任务页 → 完成基础信息和训练数据配置 → 在「梯度聚合频率」中输入正整数 → 查看配置摘要 → 点击「提交并启动训练」。
  - **系统处理流程**：接收用户输入 → 校验整数范围 → 写入训练任务配置 → 将配置传递给现有训练启动流程 → 启动任务 → 跳转至任务监控页。

- **字段规则**：

  | 字段名 | 类型 | 必填 | 规则说明 |
  |---|---|---|---|
  | 聚合方式 | 只读文本 | 是 | 首期固定显示「All-Reduce」，不提供方式切换 |
  | 梯度聚合频率 | 正整数 | 是 | 取值 1–1000，默认值为 8，单位为训练步 |
  | 配置摘要 | 只读文本 | 是 | 格式为「每 {N} 步聚合一次」，输入值变化后同步更新 |

- **交互规则**：
  - 数字输入框标题固定为「梯度聚合频率」，右侧显示单位「步」。
  - 输入框下方显示说明：「每间隔 N 个训练步执行一次梯度聚合，取值 1–1000。」
  - 值为空、非整数或超出范围时，输入框标红并显示「请输入 1–1000 的正整数」。
  - 校验未通过时，点击「提交并启动训练」不发起任务提交，焦点移至该输入框。
  - 提交成功后显示「任务已启动，聚合频率为每 {N} 步」，并跳转至「监控指标」。

- **系统规则**：
  - 仅在训练任务提交前允许修改。
  - 任务提交后保存的值作为该任务的实际运行配置，不在训练运行期间变更。
  - 将该值作为新字段附加到现有训练任务提交请求，不新增独立策略服务。
  - 训练引擎无法识别该值时，任务不得进入运行状态。

- **产品设计**：
  - 默认状态：输入框显示 8，配置摘要显示「每 8 步」。
  - 加载状态：提交时按钮显示「提交中…」并禁止重复点击。
  - 异常状态：保留已填配置，在页面顶部显示错误信息及「重试」按钮。

- **异常处理**：
  - 输入值不合法：不提交任务，显示字段级错误。
  - 任务提交失败：显示后端返回原因，保留全部配置。
  - 训练引擎不支持该值：提示「当前训练引擎不支持该聚合频率，请修改后重试」。

### 2.2 全局训练状态看板

- **页面入口**：训练任务提交成功后自动进入；或「模型训练 → 训练任务 → 查看详情 → 监控指标」。
- **核心功能**：展示当前任务的训练进度、效果指标、速度指标和训练前提交的聚合频率，便于用户观察训练表现。

- **业务流程**：
  - **用户操作流程**：进入监控页 → 查看当前聚合频率 → 查看训练进度、Loss、验证准确率和吞吐率 → 切换时间范围或放大图表查看趋势。
  - **系统处理流程**：读取任务状态和配置 → 读取现有训练指标数据 → 按时间或 Step 升序展示 → 按固定频率刷新运行中任务。

- **字段规则**：

  | 字段名 | 类型 | 必填 | 规则说明 |
  |---|---|---|---|
  | 任务状态 | 只读枚举 | 是 | 排队中、训练中、已暂停、已完成、已失败、已停止 |
  | 当前轮次 / 总轮次 | 只读整数 | 是 | 来自训练进度 |
  | 已处理样本数 | 只读整数 | 是 | 使用 K、M 缩写显示 |
  | 训练 Loss | 只读数值及序列 | 是 | 展示当前值和趋势曲线 |
  | 验证 Loss | 只读数值序列 | 否 | 未产生验证数据时显示「暂无数据」 |
  | 验证集准确率 | 只读百分比及序列 | 是 | 保留 1 位小数 |
  | Token 吞吐率 | 只读数值 | 是 | 单位为 Token/s，允许使用 K/s、M/s |
  | 梯度聚合频率 | 只读正整数 | 是 | 读取任务提交时保存的值 |

- **交互规则**：
  - 页面顶部使用状态标签、进度条和「Step {current} / {total}」展示训练进度。
  - 聚合策略区显示「当前聚合策略：All-Reduce · 每 {N} 步聚合一次」和「训练中只读」。
  - 不在监控页提供聚合频率输入框或修改按钮。
  - 图表提供「15分钟」「1小时」「全部」三个范围选项，默认为「1小时」。
  - 点击「+放大」缩小当前可见数据范围；点击「重置」恢复当前时间范围。
  - 悬停图表数据点时显示 Step 和指标精确值。

- **系统规则**：
  - 优先复用训练日志或现有指标接口已有数据，不为本需求增加自定义指标计算引擎。
  - 运行中任务默认每 10 秒刷新；非运行任务不自动刷新。
  - 指标按 Step 升序展示，缺失数据不使用前端插值伪造。
  - 页面只展示同期指标与当前聚合配置，不自动判定某项变化是否由聚合频率引起。

- **产品设计**：
  - 空状态：任务尚未产生指标时显示「训练启动后将显示监控数据」。
  - 加载状态：指标卡、图表和节点表格使用骨架屏。
  - 异常状态：保留最后数据，显示「监控数据获取失败」和「重试」。

- **异常处理**：
  - 单个指标缺失：该指标显示「暂无数据」，其他指标正常显示。
  - 请求失败：保留上次成功数据，不清空曲线。
  - 任务已结束：停止刷新，显示完整历史曲线。

#### 2.2.1 验证集准确率曲线

- **核心需求**：
  - 在当前训练任务「监控指标」页新增独立的「验证集准确率曲线」，与「全局损失函数曲线」并列展示，不新增页面或导航入口。
  - 曲线展示当前任务在各验证检查点产生的 `eval_accuracy`，用于观察模型在验证集上的准确率变化。
  - 验证集准确率与验证 Loss 是两个独立指标，不得用验证 Loss 替代准确率，也不得在前端根据 Loss 推算准确率。
  - 复用页面已有的时间范围、放大、缩小和重置能力；筛选条件同时作用于全局损失、验证集准确率及其他趋势图。

- **数据来源与口径**：

  | 数据项 | 来源/字段 | 口径规则 |
  |---|---|---|
  | 任务标识 | 当前训练任务 `task_id` | 只查询当前任务，禁止混入其他任务或其他训练轮次的数据 |
  | 训练步 | 训练引擎指标中的 `step` | 使用指标产生时的全局 Step，按整数升序展示 |
  | 指标名称 | `metric_name=eval_accuracy` | 首期复用训练引擎已有验证指标接口或结构化训练日志，不新增自定义指标计算引擎 |
  | 指标值 | `metric_value` | 后端统一输出 0–100 的百分比数值；前端仅格式化为 1 位小数和 `%` |
  | 采样时间 | `recorded_at` | 使用指标实际产生时间，用于时间范围筛选，不使用前端接收时间替代 |
  | 验证样本数 | `sample_count` | 可选返回，用于追溯本次验证的分母；不作为曲线纵轴 |
  | 数据集版本 | `dataset_version` | 可选返回，用于确认各检查点使用同一验证集版本 |

  - 训练引擎直接返回 `eval_accuracy` 时，平台原样使用；若训练引擎只返回命中数和有效样本数，则由后端按 `valid_correct / valid_total × 100%` 计算后返回。
  - `valid_total` 仅统计本次验证中实际参与 Accuracy 计算的有效样本；被训练引擎忽略或 Mask 的样本遵循训练引擎既有口径。分母为 0 时不生成数据点。
  - 多 Worker 训练只展示任务级验证结果。若引擎返回各 Worker 分片结果，应由后端汇总命中数与有效样本数后计算，不允许前端对各 Worker 百分比做简单平均。
  - 若返回值为 0–1 小数，后端转换为 0–100；前端不猜测单位。小于 0 或大于 100 的记录视为异常数据，不绘制并记录数据异常。

- **图表绘制规则**：
  - X 轴为全局 Step，按数值升序；Y 轴为验证集准确率，固定范围 0%–100%。
  - 每次完成验证并产生有效 `eval_accuracy` 时绘制一个数据点；相邻有效点使用直线连接，不使用平滑曲线生成视觉上的额外峰谷。
  - 缺失检查点不补 0、不沿用前值、不进行前端插值，折线不得跨越空值强行连接。
  - 同一 Step 出现多条成功记录时，保留 `recorded_at` 最新的一条；失败或未完成的验证记录不绘制。
  - 数据点保留圆点标识。悬停时至少展示「Step」和「验证集准确率」，准确率保留 1 位小数；返回采样时间时一并展示。
  - 当前筛选范围内只有 1 个有效点时仅显示该点，不虚构趋势；没有有效点时显示「尚未产生验证集准确率数据」。
  - 运行中任务随监控页每 10 秒刷新；已完成、已失败或已停止的任务停止轮询并保留完整历史数据。
  - 加载时使用与图表等高的骨架屏；单指标请求失败时保留上次成功曲线，并在本图表内显示「验证集准确率获取失败」和「重试」。

- **原型与交付边界**：当前原型使用静态演示点验证页面结构与交互，仅代表前端方案；是否已接入真实训练指标接口，需以后端联调记录和实际任务数据验证为准。

### 2.3 节点性能分析

- **页面入口**：当前训练任务「监控指标」页的「节点性能」区域。
- **核心功能**：以表格展示各训练节点的 GPU、显存、网络和磁盘 I/O，用户可排序并查看异常节点。

- **业务流程**：
  - **用户操作流程**：查看节点表格 → 点击性能列标题排序 → 选择「仅看异常」定位可疑节点 → 点击「刷新」获取最新数据。
  - **系统处理流程**：读取当前任务分配节点 → 获取现有节点指标 → 按用户选择的列排序 → 返回结果。

- **字段规则**：

  | 字段名 | 类型 | 必填 | 规则说明 |
  |---|---|---|---|
  | 节点名称 | 只读文本 | 是 | 当前任务实际分配的节点 |
  | GPU 利用率 | 只读百分比 | 是 | 0%–100% |
  | GPU 显存占用 | 只读数值 | 是 | 格式为「已用 / 总量 GB」 |
  | 网络接收速率 | 只读数值 | 是 | 按现有监控单位显示 |
  | 网络发送速率 | 只读数值 | 是 | 按现有监控单位显示 |
  | 磁盘 I/O | 只读数值 | 是 | 按现有监控单位显示 |
  | 状态 | 只读枚举 | 是 | 正常、指标异常、节点离线 |

- **交互规则**：
  - GPU 利用率、显存占用、网络接收、网络发送和磁盘 I/O 列支持点击表头排序。
  - 首次点击按降序排列，再次点击切换为升序。
  - 筛选框提供「全部节点」和「仅看异常」。
  - 异常节点所在行使用浅黄色背景，状态列显示异常摘要。
  - 点击「刷新」重新获取当前表格数据，不修改排序和筛选条件。

- **系统规则**：
  - 仅展示当前任务实际使用的节点，不展示集群全部节点。
  - 复用现有节点监控数据源和异常状态，不为本功能增加可配置告警规则。
  - 排序和「仅看异常」可由前端对当前已加载数据执行，不新增后端查询参数。

- **产品设计**：
  - 空状态：未分配训练节点时显示「任务开始后将显示节点性能数据」。
  - 加载状态：显示与表格行高一致的骨架屏。
  - 异常状态：表格顶部显示「节点指标获取失败」和「重试」。

- **异常处理**：
  - 单个字段缺失：对应单元格显示「暂无数据」，不使用 0 填充。
  - 单个节点离线：保留节点行，性能字段显示「—」，状态显示「节点离线」。
  - 全部节点数据获取失败：保留上次成功数据并提供重试入口。

### 2.4 验收标准

1. 创建训练任务时存在「梯度聚合频率」数字输入框，支持输入 1–1000 的正整数。
2. 点击「提交并启动训练」后，输入值被保存到任务配置并传递给训练启动流程。
3. 训练监控页只读展示本次训练实际使用的聚合频率，运行中不提供修改。
4. 看板展示当前轮次、已处理样本数、训练 Loss、验证集准确率和 Token 吞吐率。
5. Loss 和验证集准确率图表支持时间范围切换、放大、重置及数据点悬停。
6. 节点表格展示 GPU 利用率、显存占用、网络接收/发送速率和磁盘 I/O。
7. 节点表格支持列排序和「仅看异常」筛选。
8. 页面展示的聚合频率、训练效果指标、速度指标和节点指标来自同一个训练任务。
9. 验证集准确率使用当前任务真实 `eval_accuracy` 数据点，前端不以 Loss 替代、推算或补齐缺失值。
10. 验证集准确率曲线按 Step 升序展示，Y 轴固定为 0%–100%，支持时间范围、缩放、重置和数据点悬停。
11. 无有效验证数据、单点数据、异常值和请求失败时，页面分别按本节规则展示且不伪造趋势。

### 2.5 原型说明

交互原型：[`training-monitor-tuning-prototype.html`](./training-monitor-tuning-prototype.html)

原型支持以下演示：

- 在「训练前配置」中修改梯度聚合频率并完成校验。
- 点击「提交并启动训练」后，切换至「训练中观察」。
- 在监控页只读展示已提交的聚合频率。
- 切换图表时间范围、放大和重置。
- 查看独立的「全局损失函数曲线」和「验证集准确率曲线」。
- 节点性能排序、异常筛选和手动刷新。

> 注：原型内训练指标为静态演示数据，不代表真实训练指标接口已完成接入或验收。
