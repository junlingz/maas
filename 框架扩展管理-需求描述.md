# 框架扩展管理 — YAML 调试沙箱需求说明

> 版本：v1.2
>
> 规则集：`extension-params/1.0.0`
>
> 更新日期：2026-08-30
> 决策依据：2026-08-06 MaaS 需求评审保留 YAML 调试闭环；2026-08-30 在不替换调试的前提下补充独立自动化测试原型。

## 1. 产品结论与范围

页面入口：侧边栏「模型训练 → 框架扩展」。

本期保留合规名称「调试沙箱」，但其能力边界明确为：

> 用户上传单个 YAML 参数配置文件后，主动点击「开始调试」；后端调用配置校验接口，对文件、模板兼容性、参数合法性、安全性和训练字段映射进行检查，输出日志并生成版本级报告。无阻断项时才允许启用。

下列边界仅针对“调试沙箱”，独立自动化测试原型不复用或改名替代调试：

- 不上传 ZIP、TAR、Python 文件、依赖包或任意代码。
- 调试过程不创建容器、训练任务或测试任务。
- 不注入测试数据，不执行模型推理或训练。
- 不申请、占用或调度 GPU。
- 不展示训练吞吐、显存、内存峰值等未经真实执行产生的指标。
- 不把“配置校验通过”表述为“扩展代码、算法效果或训练性能验证通过”。

独立“测试”弹窗只提供单元、集成和性能三类测试工具及内置用例。兼容性和安全性仍由调试沙箱负责，不在测试弹窗重复展示。当前页面以示例执行状态体现能力；生产测试执行器、真实数据和判定阈值不在本说明的 YAML 调试规则集中。

## 2. 用户流程与状态机

```text
下载 YAML 模板
  → 修改 extension_name / parameters
  → 上传单个 YAML
  → 待调试
  → 点击“开始调试”
  → 调试中（持续输出校验日志）
  → 生成版本级报告
      ├─ 无 ERROR：调试通过 → 待测试 → 点击“开始测试” → 自动执行三类测试
      │                                      ├─ 全部通过：可启用
      │                                      └─ 存在失败：禁止启用 → 修复并重新测试
      └─ 存在 ERROR：调试未通过 → 禁止启用 → 修复并上传新版本或重新调试
```

| 状态 | 进入条件 | 可见操作 | 启用权限 |
|---|---|---|---|
| 待调试 | YAML 上传成功，尚未调用校验接口 | 开始调试、新版本 | 禁止 |
| 调试中 | 校验任务已受理，尚未完成 | 查看日志 | 禁止 |
| 调试未通过 | 任一检查返回 `ERROR` | 查看报告、重新调试、新版本 | 禁止 |
| 调试通过/待测试 | 所有调试类别无 `ERROR`，尚无通过的测试报告 | 测试、查看调试报告、重新调试、新版本 | 禁止 |
| 测试中/测试未通过 | 自动化测试执行中或存在失败用例 | 查看测试页、重新测试、新版本 | 禁止 |
| 测试通过 | 调试通过且三类自动化测试全部通过 | 启用、测试、查看报告、重新调试、新版本 | 允许 |
| 已启用 | 管理员或授权用户启用调试及测试均通过的版本 | 测试、查看报告、重新调试、新版本 | 当前版本生效 |

规则：

- 上传成功只代表文件已保存，不代表调试通过。
- 上传新版本后必须重置为「待调试」，不得复用旧版本报告。
- 同一扩展名称只能启用一个版本；启用新版本时自动停用旧版本。
- 报告必须绑定扩展 ID、版本、原文件 SHA-256 和规则集版本。
- 测试报告必须绑定扩展 ID、版本、测试运行 ID、用例版本和完成时间。

## 3. YAML 单文件契约

### 3.1 文件级要求

| 项目 | 规则 | 不满足时 |
|---|---|---|
| 文件数量 | 每次只能上传 1 个文件 | 上传失败 |
| 扩展名 | `.yaml` 或 `.yml` | 上传失败 |
| 编码 | UTF-8，可带 BOM | 上传失败 |
| 文件大小 | 1 B～64 KB | 上传失败 |
| YAML 版本 | YAML 1.2 Core Schema | 调试失败 |
| 文档数量 | 只能包含 1 个 YAML document | 调试失败 |
| 根节点 | 必须是 mapping/object | 调试失败 |
| 最大嵌套 | 6 层 | 调试失败 |
| 最大节点数 | 128 个 | 调试失败 |
| 单个文本值 | 最长 1,024 字符 | 调试失败 |

文件大小、嵌套、节点数和文本长度是平台为防止资源滥用设定的产品阈值，不是 YAML 行业标准。

### 3.2 固定结构

```yaml
schema_version: "maas.extension-params/v1"
template_id: "optimizer"
template_version: "1.0.0"
extension_name: "自适应优化器"
extension_type: "optimizer"
parameters:
  optimizer: "AdamW"
  learning_rate: 0.00002
  weight_decay: 0.01
  lr_scheduler_type: "cosine"
```

顶层仅允许：

`schema_version`、`template_id`、`template_version`、`extension_name`、`extension_type`、`parameters`。

禁止增加 `entrypoint`、`script`、`command`、`runtime`、`dependencies`、`permissions`、`credentials` 等字段。

## 4. 内置模板与参数映射

每个标准模板提供一个独立 YAML 文件，用户下载、修改参数值后再上传。

| 模板 | `template_id` | 参数 | 类型 | 允许值或范围 | 提示规则 | 训练字段映射 |
|---|---|---|---|---|---|---|
| LoRA 算法扩展 | `lora-algorithm` | `rank` | integer | 1～256，步长 1 | 8～64 为建议区间；超出仅 WARNING | `training.method.lora.rank` |
| LoRA 算法扩展 | `lora-algorithm` | `alpha` | integer | 1～512，步长 1 | 无 | `training.method.lora.alpha` |
| 自定义优化器 | `optimizer` | `optimizer` | string | AdamW、Adam、SGD、Adafactor | 无 | `training.optimizer.name` |
| 自定义优化器 | `optimizer` | `learning_rate` | float | 0.000001～1，步长 0.000001 | 无 | `training.optimizer.learning_rate` |
| 自定义优化器 | `optimizer` | `weight_decay` | float | 0～1，步长 0.001 | 无 | `training.optimizer.weight_decay` |
| 自定义优化器 | `optimizer` | `lr_scheduler_type` | string | cosine、polynomial、constant_with_warmup | 无 | `training.optimizer.scheduler` |
| 对话数据处理 | `dialogue-processing` | `maxTurns` | integer | 1～256，步长 1 | 无 | `training.data.max_turns` |
| 对话数据处理 | `dialogue-processing` | `deduplicate` | string | 关闭、精确匹配、归一化匹配 | 无 | `training.data.deduplicate` |
| 领域评估方法 | `domain-evaluation` | `threshold` | float | 0～1，步长 0.01 | 无 | `training.evaluation.threshold` |

不做字符串到数字、字符串到布尔等隐式转换；类型不一致直接判定为 `ERROR`。

## 5. 五类检查指标和判断规则

### 5.1 文件与语法检查

| 规则 ID | 检查对象 | 通过条件 | 失败条件 | 修复建议 |
|---|---|---|---|---|
| FMT-001 | 文件扩展名 | `.yaml` / `.yml` | 其他扩展名 | 重新下载 YAML 模板 |
| FMT-002 | 文件大小 | 1 B～64 KB | 空文件或超过 64 KB | 删除无关内容，不携带代码/二进制 |
| FMT-003 | 文本内容 | 有内容且不含 NUL | 空白文件或含 NUL | 用 UTF-8 文本保存 |
| FMT-004 | YAML 解析 | YAML 1.2 严格解析无错误；键唯一 | 缩进错误、重复键、多文档或解析异常 | 按错误行列修复 |
| FMT-005 | 根节点 | object/mapping | scalar 或 array | 保留模板键值结构 |
| FMT-006 | 嵌套层级 | ≤6 | >6 | 删除非模板嵌套 |
| FMT-007 | 节点数量 | ≤128 | >128 | 删除重复或未知字段 |
| FMT-008 | 单值长度 | ≤1,024 字符 | 任一 scalar 超限 | 删除大段文本、脚本或密钥 |

类别判定：任一规则失败，文件与语法检查为「未通过」。后续类别仍可返回“前序失败，无法判断”，但最终结论必须失败。

### 5.2 模板兼容性检查

| 规则 ID | 检查对象 | 通过条件 | 失败条件 |
|---|---|---|---|
| CMP-001 | `schema_version` | 等于 `maas.extension-params/v1` | 缺失或其他值 |
| CMP-002 | `template_id` | 等于用户选择的模板 ID | 缺失、未知或与所选模板不一致 |
| CMP-003 | `template_version` | 等于当前模板版本 `1.0.0` | 旧版本、未来版本或格式不一致 |
| CMP-004 | `extension_type` | 等于模板固定类型 | 用户修改或类型不一致 |
| CMP-005 | `extension_name` | 非空且 1～64 字符 | 缺失、空白或超长 |

本期不做自动迁移。模板版本不一致时必须要求重新下载当前模板，避免静默修改参数含义。

### 5.3 参数合法性检查

| 规则 ID | 检查对象 | 通过条件 | 失败条件 |
|---|---|---|---|
| PAR-001 | `parameters` | 为 object/mapping | 缺失或不是 object |
| PAR-002 | 参数白名单 | 仅包含模板声明参数 | 出现未知参数 |
| PAR-003 | 必填参数 | 模板参数全部存在且非 null | 缺失或 null |
| PAR-004 | 数据类型 | 与模板类型完全一致 | 依赖隐式转换或类型错误 |
| PAR-005 | 数值范围 | 位于闭区间内 | 小于最小值或大于最大值 |
| PAR-006 | 数值步长 | `(value-min)/step` 为整数，容差 `1e-8` | 不符合步长 |
| PAR-007 | 枚举值 | 位于模板 options 中 | 未知选项 |
| PAR-W01 | 建议区间 | 位于建议区间 | 超出建议区间时产生 WARNING，不阻断 |

### 5.4 安全性检查

后端必须使用安全 YAML loader，只构造字符串、数字、布尔、数组和对象等基础数据；不得反序列化任意语言对象。

| 规则 ID | 检查对象 | 通过条件 | 失败条件 | 风险 |
|---|---|---|---|---|
| SEC-001 | YAML 指令 | 不包含 `%YAML`、`%TAG` | 出现任一指令 | 解析行为被改变 |
| SEC-002 | 标签 | 不包含自定义/显式标签 | 出现 `!foo`、`!!python/...` 等 | 不安全反序列化 |
| SEC-003 | 锚点与别名 | 不包含 `&anchor`、`*alias`；解析器 `maxAliasCount=0` | 出现锚点或别名 | 递归/指数展开导致拒绝服务 |
| SEC-004 | 合并键 | 不包含 `<<:` | 出现合并键 | 绕过白名单或覆盖字段 |
| SEC-005 | 顶层字段白名单 | 仅包含 6 个固定字段 | 出现任何额外顶层字段 | 注入未定义行为 |
| SEC-006 | 敏感信息 | 不命中私钥、访问密钥、API Key、令牌或密码特征 | 命中 PEM 私钥、AWS Access Key、`sk-` Token 或敏感键名 | 凭证泄露 |

安全检查必须在字段映射和持久化前完成；命中任一安全规则立即产生 `ERROR`，不得允许管理员强制忽略后启用。

### 5.5 训练参数映射检查

| 规则 ID | 检查对象 | 通过条件 | 失败条件 |
|---|---|---|---|
| MAP-001 | 映射路径 | 每个模板参数都有平台登记的 `training.*` 路径 | 缺少映射或路径不合法 |
| MAP-002 | 路径唯一性 | 同一模板内一项参数对应一个唯一路径 | 两个参数写入同一路径 |
| MAP-003 | 前序结果 | 兼容性和参数检查均通过 | 任一前序类别失败 |
| MAP-004 | 请求序列化 | 映射结果可转换为 JSON 基础类型 | 存在不可序列化值 |

通过后生成只读映射预览；用户不能在 YAML 中填写或覆盖映射路径。

## 6. 统一判定规则

| 级别 | 含义 | 对总体结论影响 | 对启用影响 |
|---|---|---|---|
| `ERROR` | 文件、兼容性、参数、安全或映射不满足硬规则 | 任一 ERROR → 整体未通过 | 禁止启用 |
| `WARNING` | 满足硬规则但超出建议区间 | 整体仍可通过，报告必须展示 | 允许启用 |

总体判定公式：

```text
passed = error_count == 0
enable_allowed = passed && report.file_sha256 == current_file.sha256
```

若文件发生任何变化、SHA-256 不一致或规则集版本升级为不兼容版本，旧报告失效，状态回到「待调试」。

## 7. 后端接口与报告契约

### 7.1 开始调试

```http
POST /api/v1/framework-extension-configs/{version_id}/debug
Content-Type: application/json

{
  "file_sha256": "...",
  "rule_set_version": "extension-params/1.0.0"
}
```

返回 `202`：

```json
{
  "job_id": "debug_01...",
  "status": "running"
}
```

### 7.2 查询结果

```http
GET /api/v1/framework-extension-configs/debug-jobs/{job_id}
```

最小返回字段：

```json
{
  "job_id": "debug_01...",
  "version_id": "ext_v1...",
  "status": "passed",
  "file_sha256": "...",
  "rule_set_version": "extension-params/1.0.0",
  "error_count": 0,
  "warning_count": 0,
  "checks": [
    {
      "rule_id": "CMP-002",
      "category": "compatibility",
      "status": "passed",
      "path": "template_id",
      "message": "template_id 与所选模板一致"
    }
  ],
  "logs": ["..."],
  "mapping_preview": {},
  "started_at": "...",
  "finished_at": "..."
}
```

接口要求：

- 上传接口只做文件数量、扩展名、大小和 UTF-8 前置检查；不得自动进入调试。
- 调试接口必须幂等：同一版本、同一 SHA-256、同一规则集重复调用返回同一判定，允许生成新的运行记录。
- 后端整体校验目标：P95 ≤2 秒、超时 5 秒；超时按系统失败处理，不得判定通过。
- 校验日志和报告至少保留 180 天；版本被停用后仍可查看历史报告。
- 日志不得回显完整敏感值；发现密钥时只返回规则 ID、字段路径和脱敏摘要。

## 8. 页面交互要求

### 8.1 模板区

每个模板展示：类型、名称、说明、使用场景、配置说明、下载 YAML、上传 YAML。

下载文件必须与页面参数表、后端 Schema 和检查规则同源生成，不允许四处维护不同默认值。

### 8.2 版本表

固定列：扩展、语法、兼容、参数、安全、映射、操作。测试作为独立操作按钮进入二级工作台，不替换调试入口。

操作规则：

- 待调试：显示「开始调试」。
- 调试中：显示「调试中」，按钮禁用。
- 未通过：显示「重新调试」「报告」，不显示可用的启用操作。
- 调试通过但测试未通过：显示「测试」「重新调试」「报告」，不显示可用的启用操作。
- 调试和测试均通过：显示「启用」「测试」「重新调试」「报告」。
- 所有版本：显示「测试」「新版本」；历史版本可展开查看报告、测试和重新调试。

### 8.3 调试沙箱弹窗

必须包含：文件名、大小、扩展版本、规则集版本、五类状态、实时日志、开始/重新调试按钮。

必须显式提示：仅做 YAML 配置校验，不创建训练任务、不加载样本、不使用 GPU。

### 8.4 报告

报告必须包含：

- 总体结论、ERROR/WARNING 数量。
- 每类检查状态、检查数量、耗时和摘要。
- 每个发现的规则 ID、字段路径、错误说明、修复建议。
- 文件 SHA-256、扩展版本、规则集版本和调试时间。
- 通过时的训练字段映射预览。

### 8.5 自动化测试弹窗

必须包含：扩展名称与版本、开始/重新测试按钮、单元/集成/性能三类工具与状态、可展开的内置测试用例，以及测试结果与性能指标报告。

测试用例必须在执行前可查看，至少包含用例 ID、测试目标和预期结果；执行后同步补充实际结果、状态和耗时。

测试报告必须包含总体结论、三类测试工具与通过数量、逐项用例结果，以及启动耗时、处理吞吐、P95 延迟、内存峰值等性能指标。原型示例值必须明确标注，不得表述为生产实测数据。本期不提供测试用例下载、测试报告下载或 PDF 生成功能。

## 9. 与训练任务的集成

- 只有「已启用」版本出现在监督微调任务的扩展选择区。
- 训练任务保存扩展 ID、扩展版本、文件 SHA-256、规则集版本和规范化参数快照。
- 选择扩展后，平台按固定映射将参数回填到现有训练入参；不改变原算法执行流程。
- 验证失败、待调试、已停用版本不得用于新任务。
- 历史任务继续引用提交时的不可变快照，不随新版本或规则升级变化。

## 10. 验收标准

1. 四类模板均能下载一个 `.yaml` 文件，内容与页面参数表一致。
2. 页面只允许单个 `.yaml/.yml`，拒绝 ZIP、TAR、Python 和超过 64 KB 的文件。
3. 上传合法 YAML 后状态为「待调试」，不会自动变成通过，也不会出现训练/GPU 运行日志。
4. 点击「开始调试」后，五类检查均返回真实规则结果和规则 ID。
5. 分别构造重复键、错误模板版本、未知参数、越界参数、别名、自定义标签、私钥和未知顶层字段，均能得到对应 `ERROR` 并禁止启用。
6. `rank=4` 在硬范围内但超出建议范围时，只产生 `WARNING`，允许启用。
7. 合法优化器 YAML 能生成四个唯一训练字段映射并通过调试。
8. 调试通过后进入待测试；只有调试和自动化测试均通过才可启用。
9. 上传新版本后版本号 patch 位自增，状态回到「待调试」，旧报告仍可查看。
10. 报告下载内容与页面展示、文件 SHA-256 和当前版本一致。
11. 桌面和 390 px 宽度下无页面级横向溢出；表格允许在自身容器内横向滚动。
12. 浏览器控制台无本功能引入的错误。
13. 每个版本均显示独立「测试」按钮，原「调试」按钮和调试弹窗保持可用。
14. 测试弹窗展示单元、集成、性能三类测试工具和内置用例，点击「开始测试」后自动顺序执行并更新结果。
15. 测试前可以查看用例，测试后可以查看详细结果和性能指标报告；不提供用例、报告或 PDF 下载。

## 11. 标准依据与平台规则边界

- YAML 1.2.2 规定 mapping key 必须唯一，并定义了标签、锚点和别名等结构：<https://yaml.org/spec/1.2.2/>。
- JSON Schema Draft 2020-12 用于描述和校验对象结构；额外字段应使用 `additionalProperties: false` 明确拒绝：<https://json-schema.org/draft/2020-12>。
- OWASP 建议对不可信输入使用安全反序列化方式，避免通用 YAML loader 构造任意对象：<https://cheatsheetseries.owasp.org/cheatsheets/Deserialization_Cheat_Sheet.html>。
- 若后端使用 PyYAML，必须使用 `yaml.safe_load` 或等价 SafeLoader：<https://pyyaml.org/wiki/PyYAMLDocumentation>。

64 KB、6 层、128 节点、1,024 字符、P95 2 秒、超时 5 秒、报告保留 180 天均为本产品 v1 的可调整阈值，不应对外宣称为 YAML 或安全行业统一标准。
